#include<stdio.h>
#include<string.h>
int main()
{
    char s[101];
    int i, ln;

    scanf("%s", s);
    ln = strlen(s);

    for(i=1; i<ln; i=i+2)
    {
        if(s[i] == 'a'|| s[i] = 'e'|| s[i] == 'i' || s[i] == 'o' || s[i] == 'u')
            t = 1;
        else
    }

    return 0;
}
